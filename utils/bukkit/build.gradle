dependencies {
    compileOnly paperLegacy(PAPER_LEGACY_VERSION)
    compileOnly 'io.netty:netty-all:' + NETTY_VERSION

    api project(':nms')
    api project(':utils-common')
    api 'org.screamingsandals:adventure-text-serializer-craftbukkit:' + ADVENTURE_PLATFORM_VERSION
}