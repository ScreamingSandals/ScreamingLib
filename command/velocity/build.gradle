dependencies {
    compileOnly velocity(VELOCITY_VERSION)
    api project(':command-common')
    api project(':proxiedplayer-common')
    api group: 'cloud.commandframework', name: 'cloud-velocity', version: CLOUD_VERSION
}